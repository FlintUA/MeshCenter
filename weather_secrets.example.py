# Copy to weather_secrets.py and insert your API key(s).
# Only the key for the provider selected in web Settings -> Weather Provider
# is actually used, but both can be kept here at once.
OPENWEATHER_API_KEY = "your-openweather-api-key"
WEATHERAPI_API_KEY = "your-weatherapi-api-key"
